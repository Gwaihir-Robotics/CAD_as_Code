# Trimmed from our internal stator config stator_60mm_24n_config.yaml
# units: mm. Only the keys the fixture generators consume are kept.

outer_diameter: 60.0
inner_diameter: 36.65
slot_count: 24
tooth_width: 2.2
shoe_width: 5.0
shoe_depth: 1.3
pins:                         # alignment divots on the OD
  - angle: 0
    diameter: 1.5

# ── Side wire guides (fly winder) — consumed by SideGuideGenerator.FCMacro
side_guide:
  tip_gap: 6.00
