# Trimmed from our internal stator config stator_32mm_12n_config.yaml
# units: mm. Only the keys the fixture generators consume are kept.

outer_diameter: 32.0
inner_diameter: 15.0
slot_count: 12
tooth_width: 2.5
shoe_width: 6.10
shoe_depth: 0.5
pins:                         # alignment divots on the OD
  - angle: 0
    diameter: 2.0

# ── Side wire guides (fly winder) — consumed by SideGuideGenerator.FCMacro
side_guide:
  tip_gap: 7.10
